## Summary

<!-- What does this PR do? One or two sentences. -->

## Changes

<!-- List the key changes. Focus on CLI-visible behaviour (new flags, changed output, new commands). -->-

## Testing

- [ ] `go test ./...` passes
- [ ] `go vet ./...` passes
- [ ] Manually tested with `press <command>` (describe below)

<!-- What did you run? What was the output? -->

```
$ press 
```
