linters:
  enable:
    - revive
    - gosec
    - gocritic
    - godot
    - err113

linters-settings:
  gosec:
    excludes:
      - G306 # WriteFile with 0644 is intentional for web content files readable by all

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - err113
